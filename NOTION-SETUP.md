# 메디앤메디 노션 워크스페이스 구축 가이드

작성일: 2026-04-07
대상: 노션 처음 구축하는 스타트업

---

## 전체 구조

```
메디앤메디 워크스페이스
│
├── 📋 거래처DB ────────────── 거래처 통합 관리
│   ├── [DB] 기존거래처        ← 현재 운영중인 거래처 수동 등록
│   ├── [DB] 신규거래처(신규계약) ← 웹앱에서 자동 입력
│   ├── [DB] 팀별 업무
│   └── [DB] 계약변경
│
├── 👥 팀 스페이스
│   ├── 카페팀
│   │   ├── 링크드 뷰: 팀별 업무 (필터: 팀=카페팀)
│   │   └── 팀 메모 / 가이드
│   ├── 블로그팀
│   │   ├── 링크드 뷰: 팀별 업무 (필터: 팀=블로그팀)
│   │   └── 팀 메모 / 가이드
│   └── 디자인팀
│       ├── 링크드 뷰: 팀별 업무 (필터: 팀=디자인팀)
│       └── 팀 메모 / 가이드
│
├── 📊 경영진
│   ├── 링크드 뷰: 기존거래처 (전체 현황)
│   ├── 링크드 뷰: 신규거래처 (최근 3개월)
│   ├── 링크드 뷰: 계약변경 (최근 3개월)
│   └── 회의록
│
├── 📁 회사 운영
│   ├── 사내 규정 / 가이드
│   ├── 온보딩
│   └── 연락처
│
└── 🗂️ 아카이브
    └── 옛날 자료 정리용
```

---

## 만드는 순서 (따라하기)

### Step 1. 메인 페이지 4개 만들기 (2분)

사이드바에서 "새 페이지" 4번:

1. `거래처DB` — 아이콘: 📋
2. `팀 스페이스` — 아이콘: 👥
3. `경영진` — 아이콘: 📊
4. `회사 운영` — 아이콘: 📁

### Step 2. 거래처DB 안에 데이터베이스 4개 (5분)

`거래처DB` 페이지 열고:

**① 기존거래처 DB** (수동 등록용)
1. 빈 줄에 `/데이터베이스` 입력
2. "데이터베이스 - 전체 페이지" 선택
3. 제목: `기존거래처`
4. 아래 속성 직접 추가 (속성 추가 방법: 테이블 헤더 우측 `+` 클릭)

| 속성명 | 타입 | 설명 |
|--------|------|------|
| 치과명 | Title | 기본 제목 (이미 있음) |
| 원장명 | 텍스트 | |
| 지역 | 선택 | 서울/경기/부산 등 |
| 상세주소 | 텍스트 | |
| 계약상태 | 선택 | `운영중` / `일시중단` / `해지` |
| 계약시작일 | 날짜 | |
| 현재서비스 | 다중선택 | 카페바이럴, 블로그작성 등 |
| 월계약금 | 텍스트 | 예: 150만원 |
| 담당자 | 텍스트 | 메디앤메디 내부 담당자 |
| 메모 | 텍스트 | 특이사항 |

→ 현재 계약중인 거래처를 여기에 수동으로 하나씩 등록.
→ 나중에 신규거래처가 계약 확정되면 여기로 옮기는 흐름.

**② 신규거래처(신규계약) DB** (웹앱 자동 입력)
1. `거래처DB` 페이지로 돌아가기
2. `/데이터베이스` → "데이터베이스 - 전체 페이지"
3. 제목: `신규거래처(신규계약)`
4. 기본 속성 그대로 두기 (앱에서 자동으로 채움)

**③ 팀별 업무 DB**
1. `거래처DB` 페이지로 돌아가기
2. `/데이터베이스` → "데이터베이스 - 전체 페이지"
3. 제목: `팀별 업무`

**④ 계약변경 DB**
1. `거래처DB` 페이지로 돌아가기
2. `/데이터베이스` → "데이터베이스 - 전체 페이지"
3. 제목: `계약변경`

### Step 3. Integration 연결 (1분)

**4개 DB 각각:**
1. DB 열기
2. 우측 상단 `···` 클릭
3. "연결" → 만든 Integration 선택 → 확인

⚠️ **4개 다 해야 합니다. 하나라도 빠지면 403 에러.**

### Step 4. DB ID 복사 (1분)

각 DB 열고 브라우저 주소창에서:
```
https://www.notion.so/회사이름/신규거래처-신규계약-abc123def456789012345678abcdef12?v=...
```
마지막 하이픈(-) 뒤부터 물음표(?) 앞까지 = DB ID (32자리)

**4개 DB ID를 메모해두기:**
- 기존거래처 DB ID → (지금은 앱 연동 없음, 나중에 사용)
- 신규거래처 DB ID → `NOTION_MAIN_DB_ID`
- 팀별 업무 DB ID → `NOTION_TASK_DB_ID`
- 계약변경 DB ID → `NOTION_CHANGE_DB_ID`

### Step 5. 팀 스페이스 만들기 (3분)

`팀 스페이스` 페이지 열고:

**카페팀 페이지:**
1. `/페이지` → 제목: `카페팀`
2. 카페팀 페이지 안에서 `/링크된` (또는 `/linked view`)
3. "데이터베이스 링크드 뷰" → `팀별 업무` 선택
4. 필터 추가: `팀` = `카페팀`
5. 이 뷰 이름: "카페팀 업무"

**블로그팀, 디자인팀도 동일하게 반복.**
- 블로그팀 → 필터: 팀=블로그팀
- 디자인팀 → 필터: 팀=디자인팀

→ 이제 각 팀은 자기 페이지에서 자기 업무만 보임.
→ 웹앱에서 신규거래처 제출하면 팀별 업무가 자동 생성되고, 각 팀 페이지에 바로 뜸.

### Step 6. 경영진 페이지 (3분)

`경영진` 페이지 열고:

1. `/링크된` → `기존거래처` 선택
   - 필터: 계약상태 = 운영중
   - 정렬: 계약시작일 내림차순
   - 이 뷰 이름: "운영중 거래처"

2. `/링크된` → `신규거래처(신규계약)` 선택
   - 정렬: 생성일 내림차순
   - 이 뷰 이름: "최근 신규거래처"

3. `/링크된` → `계약변경` 선택
   - 정렬: 생성일 내림차순
   - 이 뷰 이름: "최근 계약변경"

→ 대표/이사가 이 페이지 하나만 보면: 현재 몇 개 거래처 운영중인지, 신규는 몇 건 들어왔는지, 변경은 뭐가 있는지 한눈에.

### Step 7. 회사 운영 페이지 (나중에)

지금은 빈 페이지로 두고, 필요할 때 채우면 됩니다:
- 사내 규정
- 신입 온보딩 체크리스트
- 거래처 관리 매뉴얼
- 마케팅 서비스 단가표

---

## 거래처 생애주기 (전체 흐름)

```
미팅 → 웹앱 입력 → [신규거래처 DB]에 자동 저장
                         │
                         ├→ [팀별 업무 DB]에 업무 카드 자동 생성
                         │   (카페팀/블로그팀/디자인팀 각각)
                         │
                         ▼ 계약 확정 시
                    [기존거래처 DB]에 수동 등록
                    (상태: 운영중)
                         │
                         ├→ 서비스 변경 시 → 웹앱 계약변경 → [계약변경 DB]
                         ├→ 해지 시 → 기존거래처 상태를 "해지"로 변경
                         │
                         ▼ 어디서든
                    링크드 뷰로 필터링해서 조회 가능
```

**기존거래처 DB = 회사의 "고객 명부"**
- 현재 돈 내고 있는 거래처 전체 현황
- 신규거래처는 미팅/상담 단계, 기존거래처는 계약 확정 단계
- 나중에 두 DB를 Relation으로 연결하면 "이 거래처가 처음 미팅할 때 뭘 말했는지"도 추적 가능

---

## 팀별 업무 DB 속성 (자동 생성됨)

앱에서 신규거래처 제출 시 자동으로 만들어지는 업무 카드:

| 속성 | 타입 | 예시 |
|------|------|------|
| 제목 | Title | 신규: 해피치과 - 카페바이럴 10건/월 |
| 팀 | Select | 카페팀 / 블로그팀 / 디자인팀 |
| 상태 | Select | 대기 / 진행중 / 완료 |
| 거래처 | Relation | → 신규거래처 DB 연결 |
| 생성일 | Date | 자동 |
| 상세 내용 | Rich Text | 자동 생성 |

이 속성들은 앱 코드에서 자동으로 만들어줍니다.
처음에 DB가 비어있어도 괜찮습니다.

---

## 링크드 뷰란?

원본 DB는 `거래처DB` 안에 **딱 하나**만 있음.
다른 페이지에서 "링크드 뷰"로 가져오면:
- 같은 데이터를 다른 필터/정렬로 볼 수 있음
- 어디서 수정해도 원본에 반영됨
- 카페팀이 보는 것, 경영진이 보는 것, 전략기획팀이 보는 것 = 전부 같은 데이터

→ "어느 페이지에서든 거래처를 끌어올 수 있게" = 이것.

---

## 기존거래처 첫 등록 팁

현재 운영중인 거래처를 한번에 넣는 방법:

**방법 1. 노션에서 직접 입력** (10개 이하)
- DB 열고 한 줄씩 추가

**방법 2. CSV 가져오기** (10개 이상)
1. 엑셀/구글시트에 거래처 목록 정리 (치과명, 원장명, 지역, 서비스, 월계약금...)
2. CSV로 저장
3. 기존거래처 DB에서 `···` → "가져오기" → CSV 선택
4. 열 매핑 확인 → 완료

→ 기존 거래처가 20~30개 정도면 엑셀에 30분 정리 후 CSV 임포트가 제일 빠름.

---

## 전체 소요 시간

| 단계 | 시간 |
|------|------|
| 메인 페이지 4개 | 2분 |
| DB 4개 생성 | 5분 |
| 기존거래처 속성 설정 | 3분 |
| Integration 연결 | 1분 |
| DB ID 복사 | 1분 |
| 팀 스페이스 (링크드 뷰) | 3분 |
| 경영진 페이지 | 3분 |
| **합계** | **~18분** |
| 기존거래처 등록 (선택) | 30분~1시간 |

---

## Vercel 환경변수 (업데이트)

DB ID 4개 + API 키:

```
NOTION_API_KEY        = ntn_만든키값
NOTION_MAIN_DB_ID     = 신규거래처(신규계약) DB ID
NOTION_TASK_DB_ID     = 팀별 업무 DB ID
NOTION_CHANGE_DB_ID   = 계약변경 DB ID
NOTION_EXISTING_DB_ID = 기존거래처 DB ID (향후 확장용)
```

---

## 다음 단계

DB ID 4개를 알려주시면:
1. 앱 코드에서 데모 모드 → 실제 Notion 연동 전환
2. Vercel 재배포
3. 실제 데이터가 노션에 들어가는지 테스트
