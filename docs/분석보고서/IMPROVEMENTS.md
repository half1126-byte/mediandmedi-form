# 메디앤메디 앱 개선 사항

리뷰 일자: 2026-04-07
리뷰 기준: CEO Review + Design Review 결과

---

## 1. [Bug] 계약변경 API 미연결

**파일:** `src/app/contract-change/page.tsx:33-46`
**현상:** `handleSubmit`이 `alert()`만 띄우고 끝남. `/api/change` API를 호출하지 않음.
**영향:** 계약변경 데이터가 실제로 저장되지 않음. 경영진 데모에서 문제됨.
**수정:**
- `fetch('/api/change')` 호출 추가
- 응답에서 pageId 받아서 localStorage에 저장
- 성공 시 요약 또는 완료 화면으로 이동
- 에러 처리 (재시도 버튼 포함)

---

## 2. [Bug] 요약 페이지 데이터 불완전

**파일:** `src/app/summary/page.tsx:123-128`
**현상:** step1, step2, step5만 표시. step3(시설/장비), step4(브랜딩), step6(계약 상품) 누락.
**영향:** 제출 후 "내가 입력한 게 다 들어간 건가?" 불안감 유발.
**수정:**
- step3 (체어 수, 장비, 시설, 주차) 요약 추가
- step4 (한줄소개, 타겟환자, 차별점) 요약 추가
- step6 (계약 서비스, 패키지, 월계약금) 요약 추가
- Step7(최종확인)과 동일한 구조로 통일

---

## 3. [UX] 요약 페이지 PIN 평문 노출

**파일:** `src/app/summary/page.tsx:204`
**현상:** `PIN: {pin}`이 화면 하단에 항상 노출됨.
**영향:** 원장님 앞에서 보안 관련 어색함. 공유 시 불필요한 정보.
**수정:**
- PIN을 "보기/숨기기" 토글로 변경
- 기본 상태는 마스킹 (****)
- 탭하면 잠시 표시 후 다시 숨김

---

## 4. [Bug] Step7 요약에서 빈 배열이 빈 문자열로 표시

**파일:** `src/app/new-clinic/page.tsx:817-825`
**현상:** `SummaryItem`이 value가 빈 문자열("")이면 null 반환하지만, 빈 배열을 `join(', ')`하면 빈 문자열이 됨.
**영향:** 미입력 항목이 라벨만 표시되고 값이 비어보임.
**수정:**
- `SummaryItem`에서 빈 문자열/공백만 있는 경우도 null 반환하도록 수정
- 배열 기반 값은 길이 체크 후 전달

---

## 5. [UX] 계약변경 완료 후 결과 저장 및 이동

**파일:** `src/app/contract-change/page.tsx`
**현상:** 성공 시 alert 후 홈으로 이동. 제출 결과를 확인할 수 없음.
**영향:** "제출이 된 건가?" 불확실함.
**수정:**
- 제출 성공 시 결과를 localStorage에 저장
- 완료 메시지를 인라인으로 표시 (alert 제거)
- 홈으로 이동 버튼 제공

---

## 구현 순서

1. 계약변경 API 연결 (#1, #5)
2. 요약 페이지 전체 데이터 (#2)
3. PIN 토글 (#3)
4. Step7 빈 값 처리 (#4)
