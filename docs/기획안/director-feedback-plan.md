# 이사님 피드백 적용 계획서

작성일: 2026-04-09  
방법론: Superpowers — Writing Plans + Verification Before Completion

---

## 영향 범위 요약

| 영역 | 파일 | 변경 규모 |
|------|------|----------|
| 진료과목 데이터 (큰 폭 개편) | `src/data/dental.ts` | 전체 재작성 |
| 폼 타입 정의 | `src/app/new-clinic/page.tsx` | `FormData` 인터페이스 확장 |
| Step1 (기본 정보) | Step1 컴포넌트 | 의료진 수만큼 입력칸 동적 생성 |
| Step2 (진료) | Step2 컴포넌트 | 진료과목 카테고리 전면 개편, 임플란트 재료 그룹 추가 |
| Step3 (시설/장비) | Step3 컴포넌트 | 인테리어 컨셉 → Step4로 이동, 임플란트 제품사 삭제, 장비 항목 변경 |
| Step4 (브랜딩) | Step4 컴포넌트 | 홍보 포인트 6개 필드 추가, 봉직의 삭제, 로고/브랜드컬러 추가 |
| Step5 (마케팅) | Step5 컴포넌트 | 유입경로 변경, DID 정보 이동, 리뷰 증정선물 추가 |
| Step6 (계약) | Step6 컴포넌트 | 서비스 목록 전면 교체 (4개 팀, 30+ 서비스), DID 대수 삭제 |
| 서비스 데이터 | `src/data/services.ts` | 전체 재작성 (10개 → 30+개) |
| Notion 매핑 | `src/lib/notion.ts` | `buildMainProperties` 신규 필드 매핑 |
| Notion DB 속성 | (수동 작업) | 신규 컬럼 추가 필요 |

---

## 1. 신규개원 — Step1 (기본 정보)

### 변경
- **총 의료진 수에 따라 성함/직함/전문의 입력칸 동적 생성**
  - 현재: `doctorCount` 숫자만 받음 (Step4에 `additionalDoctors` 배열 별도)
  - 변경: 의료진 N명 → Step1에서 `[{ name, title, specialty }]` 배열 N개 생성
  - 첫 번째는 기존 "원장님 성함" 그대로 사용 (대표 원장)
  - 추가 봉직의는 의료진 수 - 1만큼 입력칸 표시

### 영향 코드
- `FormData.step1` 에 `doctors: { name; title; specialty }[]` 추가
- `Step4.additionalDoctors` 삭제 → Step1로 통합
- `buildMainProperties` 봉직의 매핑 위치 변경

---

## 2. 신규개원 — Step2 (진료과목)

### 카테고리 변경 (BEFORE → AFTER)

| 카테고리 | 변경 내용 |
|---------|----------|
| ~~일반/기본~~ | **삭제** → 신설 "일반진료"로 흡수 |
| **임플란트** | `원데이 임플란트` 삭제, `전악 임플란트` → `전체 임플란트`로 변경 |
| **임플란트(재료)** | 신설 — 메이커별 모델 다중 선택 |
| **보철** | `크라운(씌우기)` → `크라운`, `부분 틀니` 삭제, `전체 틀니` 삭제 |
| **일반진료** | 신설 — 구강검진/사랑니발치/충치치료 등 10개 |
| **턱관절치료** | 신설 — 보톡스/물리/스프린트/약물/PDRN |
| **교정** | 인비절라인/세라핀/매직어라인 + 클리피씨/세라믹/메탈/부분/설측/소아교정(프리올소/펜듈럼/페이스마스크 등) |
| **심미치료** | `심미` → `심미치료`, 항목 변경 (치아미백/라미네이트/레진/앞니재치료/잇몸성형/잇몸착색제거/아이콘레진/기타) |
| **소아진료** | `소아/예방` → `소아진료`, 항목 변경 (영유아구강검진/학생구강검진/불소도포/웃음가스치료/유치발치/구강위생교육/예방치료/기타) |
| ~~구강외과/전문~~ | **삭제** |
| **기타** | `응급치료`, `장애인치료`만 남김 |

### 신설: 임플란트(재료) 그룹 (계층 구조)

오스템:
- SOI, BA, HA, CA, SA, 기타

메가젠:
- 블루 다이아몬드, 애니원, ST

기타 메이커:
- 스트라우만, 덴티움, 디오 임플란트, 덴티스, 네오 임플란트, 기타

### 신설: 교정 — 투명교정 하위 계층

투명교정:
- 인비절라인, 세라핀, 매직어라인, 기타

소아교정 하위:
- 프리올소, 펜듈럼, 페이스마스크, 기타

### 데이터 구조 결정 필요

**선택 1 (단순):** `dentalSubjects: string[]` 배열에 모든 항목 평면화
- 장점: 기존 구조 유지
- 단점: 계층 정보 손실 ("인비절라인"이 어떤 카테고리인지)

**선택 2 (계층):** `dentalSubjects: { category, item, subItem? }[]` 
- 장점: 카테고리 정보 보존
- 단점: 마이그레이션 + Notion 저장 시 평면화 필요

**권장:** 선택 1 (단순) — Notion에 multi-select로 저장하기 쉽고, UI는 카테고리 그룹으로 렌더링만 하면 됨

---

## 3. 신규개원 — Step3 (시설/장비)

### 장비 (EQUIPMENT_LIST)
- **변경:** 인터뷰 자료 내용으로 재작성 필요 → **이사님께 인터뷰 자료 요청** 필요
- 현재: `CT(CBCT)`, `구강스캐너`, `디지털X선` 등 14개

### 시설 (FACILITY_LIST)
- **변경:** `수술실, 회복실, 턱관절치료실, VIP룸, 소아진료실, 소아대기실, 수유실, 장애인시설, 감염관리실`
- 추가: `턱관절치료실`, `소아진료실`, `소아대기실`
- 삭제: `상담실`, `키즈존` (소아진료실/소아대기실로 분화됨)

### 임플란트 제품사
- **삭제** → Step2의 `임플란트(재료)` 그룹으로 이동

### 인테리어 컨셉
- **이동:** Step3 → Step4

---

## 4. 신규개원 — Step4 (브랜딩 & 철학)

### 변경 사항

**기존:**
- oneLiner, philosophy, targetPatients, differentiator
- doctorCareer, hasProfilePhoto, additionalDoctors

**변경 후:**
- 의료진 홍보 포인트
- 병원 홍보 포인트
- 주요진료 홍보 포인트
- 진료 철학 포인트
- 병원 입지 및 타겟
- **인테리어 컨셉** (Step3에서 이동)
- 프로필 사진 보유 (유지)
- **로고파일 보유 + 첨부파일** (신규)
- **브랜드 컬러** (신규)
- ~~봉직의 정보~~ (삭제 — Step1으로 이동)

### 첨부파일 처리

**현재 앱 제약:** 폼은 Notion REST API + JSON 직렬화 전제 → 파일 업로드 미지원

**구현 방안:**
- A) Vercel Blob에 업로드 → 공개 URL 저장 (텍스트로)
- B) "로고 파일이 있으시면 카카오톡으로 보내주세요" 안내문만 표시 (체크박스만)
- **권장: B → A 단계적**: 1차는 체크박스 + 텍스트, 2차에 Vercel Blob 도입

---

## 5. 신규개원 — Step5 (마케팅 방향)

### 유입경로 변경
**기존:** `지인 소개, 인터넷 검색, 기존 거래처 추천, 영업 방문, 세미나/학회, SNS, 기타`  
**변경:** `컨설팅 소개, 지인 소개, 인터넷 검색, 세미나/학회, SNS, 기타`

- "소개" 클릭 시 이름 입력칸 표시 (조건부 렌더링)

### 추가 항목
- 개원이벤트 계획 하단에 **DID 정보 작성칸** (Step6에서 이동)
- 네이버리뷰/카톡채널 추가시 **증정선물 작성칸** (조건부)

---

## 6. 신규개원 — Step6 (계약 서비스)

### 전면 교체 (서비스 데이터)

#### 마케팅팀 (18개)
플레이스 상위노출, 플레이스 관리, 당근 바이럴, 당근 후기+단골, 카페 바이럴, 카페 댓글 붙이기, 블로그 지수작업, 블로그 상위노출, 언론보도, 영수증 리뷰, 예약 리뷰, 인스타그램, 환자경험관리, 검색광고, 자동완성, 지식인, 체험단, 개원셋팅

#### 바이럴팀 (5개)
정보성 블로그, 임상 블로그, 카페 제휴-정보성, 카페제휴-임상, 블로그 키워드 관리

#### 디자인팀 (4개)
사진촬영, 영상촬영, 유튜브, 디자인 관리

#### 웹팀 (3개)
홈페이지 제작, 홈페이지 관리, 팝업관리

**총 30개 서비스, 4개 팀**

### 삭제
- DID 설치대수
- DID 위치정보 (→ Step5로 이동)

---

## 7. Notion DB 매핑 (수동 작업 필요)

### 거래처 메인 DB 신규 컬럼
| 컬럼명 | 타입 | 비고 |
|--------|------|------|
| `의료진정보` | Rich Text | name/title/specialty 직렬화 |
| `의료진홍보포인트` | Rich Text | |
| `병원홍보포인트` | Rich Text | |
| `주요진료홍보포인트` | Rich Text | |
| `진료철학포인트` | Rich Text | |
| `병원입지타겟` | Rich Text | |
| `로고파일보유` | Checkbox | |
| `브랜드컬러` | Rich Text | |
| `소개자명` | Rich Text | 유입경로=소개일 때 |
| `리뷰증정선물` | Rich Text | |
| `DID정보` | Rich Text | (Step6에서 Step5로 이동했지만 컬럼은 유지) |

### 삭제 가능 컬럼
- `인테리어` (이동만, 컬럼은 유지)
- `봉직의정보` (Step1으로 이동, 컬럼은 유지)
- `임플란트제품사` (Step2 진료과목으로 흡수, 컬럼은 유지)
- `DID설치대수` (삭제 가능)

> **권장: 컬럼은 삭제하지 말고 유지** — 기존 데이터 보존 + 필요 시 표시 가능

---

## 작업 우선순위 (5단계 점진적 배포)

### Phase 1: 데이터 정의 (30분)
- `src/data/dental.ts` 전면 재작성 (카테고리, 항목, 임플란트 재료)
- `src/data/services.ts` 전면 재작성 (4팀, 30개 서비스)
- 빌드 검증

### Phase 2: Step1 의료진 동적 입력 (1시간)
- `FormData.step1.doctors` 배열 추가
- Step1 UI: 의료진 수 → N개 입력 그룹 동적 렌더링
- Step4 `additionalDoctors` 제거
- 빌드 + UI 테스트

### Phase 3: Step2 진료과목 개편 (1.5시간)
- 카테고리 그룹 UI 재작성
- 임플란트(재료) 계층 그룹 추가
- 교정 — 투명교정/소아교정 하위 계층 추가
- 빌드 + UI 테스트

### Phase 4: Step3/4/5 항목 정리 (1시간)
- Step3: 시설/장비 항목 변경, 임플란트 제품사 삭제, 인테리어 → Step4로 이동
- Step4: 홍보 포인트 6개 + 로고 + 브랜드컬러 + 인테리어 컨셉
- Step5: 유입경로 변경, DID 정보 추가, 증정선물
- 빌드 + UI 테스트

### Phase 5: Step6 서비스 + Notion 매핑 (1시간)
- Step6 서비스 그룹 UI (4팀)
- `buildMainProperties` 신규 필드 매핑
- Notion DB 컬럼 추가 안내
- 빌드 + 통합 테스트

---

## 사전 확인 필요 사항 (이사님)

1. **인테리어 컨셉** — Step3 시설/장비에서 Step4 브랜딩으로 이동, 입력 형식은 자유 텍스트 유지인지?
2. **장비 인터뷰 자료** — 어떤 항목으로 변경할지 자료 필요
3. **로고 파일 첨부 방식** — Vercel Blob(자동 업로드) 사용 가능 여부 (개발팀 환경변수 필요)
4. **소아교정 하위 항목** — `프리올소 / 펜듈럼 / 페이스마스크`만인지, 다른 항목 있는지
5. **임플란트(재료) 메가젠 ST** — `애니원, ST` 표기가 `애니원 ST` 한 항목인지 둘인지
6. **네이버리뷰/카톡채널 증정선물** — 어떤 마케팅 채널 옵션 선택 시 표시할지 정확한 트리거 조건

---

## 예상 총 작업 시간

- **개발:** 4~5시간
- **이사님 확인 대기 + 인터뷰 자료 정리:** 별도

---

## 검증 체크리스트 (Verification Before Completion)

각 Phase 완료 시:
- [ ] `next build` exit 0
- [ ] `npx eslint src/` 0 errors
- [ ] 로컬 dev 서버에서 해당 Step UI 렌더링 확인
- [ ] 다음 Step으로 이동 + 데이터 유지 확인
- [ ] 제출 시 Notion 페이지 정상 생성 확인 (테스트 DB)
